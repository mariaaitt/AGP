package com.traveloffersystem.config;

import org.springframework.context.annotation.Configuration;

/**
 * 项目的根配置（如果你想放一些公共 @Bean，可以写在这里）
 */
@Configuration
public class AppConfig {
    // 例如：@Bean @Scope("singleton") ...
    // 如果暂时没有额外的Bean，也可为空
}
